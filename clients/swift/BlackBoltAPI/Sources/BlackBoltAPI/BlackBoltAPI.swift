import Foundation
import OpenAPIRuntime
import OpenAPIURLSession

public enum BlackBoltAPI {
    // Placeholder until generated client types are emitted from openapi.yaml.
    public static func makeTransport() -> ClientTransport {
        URLSessionTransport()
    }
}
